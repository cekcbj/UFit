<%= simple_form_for @workout do |f| %>
  <%= f.input :name, :hint => 'Name your workout'  %>
  <%= f.input :schedule, as: :schedule %><br>
  <%= f.button :submit, :disable_with => 'Please wait ...' %>
  <% end %>
